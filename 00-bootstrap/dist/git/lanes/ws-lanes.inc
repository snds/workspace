# snds-workspace git lanes (H18). Generated by 09-tools/git_lanes.py render; do not edit.
# Installed only by a human: workspace-doctor.sh --install-git-hooks (included from ~/.gitconfig).
# Global config-based hooks (git >= 2.54). Each lane execs the pinned bin/ws-hook, which runs the
# pinned git_lanes.py; with no pin the lane allows. Audit: python3 09-tools/git_lanes.py audit.
[hook "ws-lane-pre-commit"]
	command = "W=\"$HOME/.config/snds-workspace/bin/ws-hook\"; [ -x \"$W\" ] || exit 0; exec env -u PYTHONPATH -u PYTHONHOME -u PYTHONSTARTUP -u PYTHONINSPECT -u PYTHONUSERBASE \"$W\" lane pre-commit"
	event = pre-commit
	enabled = true
[hook "ws-lane-commit-msg"]
	command = "W=\"$HOME/.config/snds-workspace/bin/ws-hook\"; [ -x \"$W\" ] || exit 0; exec env -u PYTHONPATH -u PYTHONHOME -u PYTHONSTARTUP -u PYTHONINSPECT -u PYTHONUSERBASE \"$W\" lane commit-msg"
	event = commit-msg
	enabled = true
[hook "ws-lane-pre-merge-commit"]
	command = "W=\"$HOME/.config/snds-workspace/bin/ws-hook\"; [ -x \"$W\" ] || exit 0; exec env -u PYTHONPATH -u PYTHONHOME -u PYTHONSTARTUP -u PYTHONINSPECT -u PYTHONUSERBASE \"$W\" lane pre-merge-commit"
	event = pre-merge-commit
	enabled = true
[hook "ws-lane-pre-push"]
	command = "W=\"$HOME/.config/snds-workspace/bin/ws-hook\"; [ -x \"$W\" ] || exit 0; exec env -u PYTHONPATH -u PYTHONHOME -u PYTHONSTARTUP -u PYTHONINSPECT -u PYTHONUSERBASE \"$W\" lane pre-push"
	event = pre-push
	enabled = true
[hook "ws-lane-prepare-commit-msg"]
	command = "W=\"$HOME/.config/snds-workspace/bin/ws-hook\"; [ -x \"$W\" ] || exit 0; exec env -u PYTHONPATH -u PYTHONHOME -u PYTHONSTARTUP -u PYTHONINSPECT -u PYTHONUSERBASE \"$W\" lane prepare-commit-msg"
	event = prepare-commit-msg
	enabled = true
[hook "ws-lane-post-commit"]
	command = "W=\"$HOME/.config/snds-workspace/bin/ws-hook\"; [ -x \"$W\" ] || exit 0; exec env -u PYTHONPATH -u PYTHONHOME -u PYTHONSTARTUP -u PYTHONINSPECT -u PYTHONUSERBASE \"$W\" lane post-commit"
	event = post-commit
	enabled = true
