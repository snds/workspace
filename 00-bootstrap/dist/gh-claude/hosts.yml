# Claude-only gh config (Claude surfaces are personal-only). Names the snds account;
# the token stays in the OS keyring (gh auth login as snds once per machine).
# No secrets in this file.
github.com:
    users:
        snds:
    git_protocol: ssh
    user: snds
