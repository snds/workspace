---
title: Knowledge vault index
tags: [knowledge-vault, index]
created: 2026-04-28
updated: 2026-09-22
---

# Knowledge Vault — Index

Navigable index of all entries in `08-knowledge/`. Updated by Claude at the end of sessions
when new entries are written. Entries are grouped by domain, then listed alphabetically.

---

## Design

- [[ai-and-design-systems]] — **Brad Frost / Southleft canon absorbed.** DS = critical UI infrastructure (stool + people/process); AI is mortar not a parallel universe; "Is it good?" once generation is cheap; steel curtain (CI/axe/evals, not LLM-as-judge); context-based DS; agents as users (HTML+markdown); gen UI = JSON+recipes+confidence; org arc sell→pilot→rollout→govern. Operating model [[18-design-systems-ai-operating-model]]; procedure [[ai-design-systems]]. What was already in the vault vs what the 2026 course added. Triggers: `brad frost`, `atomic design`, `ai as mortar`, `steel curtain`, `on-rails`, `off-rails`, `context-based design system`, `generative ui`, `agents as users`, `three-legged stool`, `vibe coding`. (2026-09-11)
- [[agent-output-rails]] — Onori's DS-eval method, minus the tester. Isolation (measure docs/catalog with chunks/lint/MCP off), pack recipes instead of a wizard, product-repo lint, complementary a11y proxies. Capture is `vqa capture` in [[visual-prove-engine]], not a project script. Do not clone `design-system-agent-tester`. Triggers: `assistance off`, `ds evals`, `agent tester`, `docs flywheel`, `design system chunks`, `onori evals`, `vqa capture` (2026-09-03)
- [[agentic-ds-context-model]] — **Remap of the vault ontology / graphs / Wolosin context model onto 2026 agentic specs.** Three graphs must not mix (skill load · epistemic · domain artifact; DSDS for DS). Complements stay complements. Project-independent constitution at [[dsds-constitution]]; standing methods at [[idempotent-design-decisions]]. Generalized to other job contexts in [[agentic-domain-constitutions]] / [[domain-constitutions]]. Source review via `ds-source-watch`. Triggers: `ontology`, `knowledge graph`, `context model`, `agentic design system`, `dsds`, `context engineering`, `harness`, `for: agent`, `combos`, `source watch` (2026-09-01; generalized 2026-09-02)
- [[display-graphic-motion-systems]] — **Register before tokens.** Product chrome, graphic system, diegetic/film UI, programmatic video, and look-dev do not share duration tables. Compose so the still reads at distance, then animate the few layers that earn motion. SVG: transform first, mask/stroke for reveal and draw, morph only when meaning changes. React UI default is Motion (`motion/react`). Picard/Okuda/GMunk are testimony; LCARS is one pack. Live SVG emit via [[gd-generation-tooling]]; flattened plates are not construction. Pixel-frame pass of the three study videos is still open. Triggers: `film ui`, `diegetic ui`, `svg animation`, `display graphics`, `graphic system motion`, `hud illustration`, `playback graphics`, `programmatic video`, `live svg`, `display grammar`, `flattened png` (2026-09-02)
- [[llm-safe-design-system-expressiveness]] — When an LLM authors UI, off-system values must be **inexpressible**, not discouraged. Docs/`CLAUDE.md` are probabilities; **CI is the contract**. Author in decisions (`background-card`), not values (`p-4`, hex). Escape hatches (raw layout utilities, arbitrary Tailwind, a second `dark:` pass, growing `eslint-disable`) are cracks. Polar Orbit is testimony for the law, not a StyleX/`Box` import. Complements [[component-contracts-and-schemas]] (arbitration) and [[contracts-first-delivery]] (boundaries). Triggers: `llm-safe`, `inexpressible`, `off-system`, `CI is the contract`, `typed tokens`, `escape hatch`, `light-dark`, `no raw layout`, `Orbit` (2026-09-02)
- [[component-contracts-and-schemas]] — **The arbitration layer above component documentation.** Description vs contract vs schema vs spec (_a description informs; a contract arbitrates_); the **seven gates** restated as pass/fail tests (well-typed · normalized · independent · verifiable · deterministic · efficient · evolvable); the **arbitration test**, the **deletion test**, and the **LLM boundary trap** (verification stops where an agent smooths the artifact); _an untransformed tool export is **testimony**, not a contract_; the investment gate (when NOT to build one) and the L0–L4 maturity ladder; and the six places our own stack currently fails the gates. Plus the **wider field** (Curtis/Specs · Vallaure/DS Contracts · Morales Achiardi · Pitre · Onori/DSDS) and its reconciliations: contracts are the _interconnections_ not the elements; **neither implementation may update the other directly**; _authority belongs to whatever layer can refuse deterministically_; **AI at authoring time, determinism at run time**; the **contract boundary** (outcome not technique, composition not usage); the mid-size capacity trap; and the format landscape (DTCG=values · Specs/DS Contracts=facet 18 · DSDS=facets 1–17). **Replacement rule:** a legacy feature inventory is testimony too — derive articulation-downward (purpose → required behavior → contract → components), demote prior art to pitfall-ledger/effort-calibration/parity-horizon consulted _after_ the draft, and gate every feature on cited provenance ("the old system has it" is not provenance). **Growth-horizon rule:** _scope down, don't shape down_ — the entry segment sets the **spec**, the growth horizon sets the **schema** (they version separately for exactly this); **model the axis, ship one value on it**; record decisions not to build (`enabled: false` + rationale); and keep **capability and entitlement orthogonal** because tier boundaries move. Triggers: `component contract`, `contract`, `schema`, `spec`, `component spec`, `arbitrate`, `single source of truth`, `deterministic`, `verifiable`, `normalized`, `platform neutral`, `adr`, `schema version`, `component as data`, `specs`, `testimony`, `dsds`, `browser-driven`, `consumer-controlled`, `variant delta`, `state classification`, `three-way differ` (2026-07-28)
- [[ds-parent-owns-shared-defects]] — **Name the layer before the first edit.** Shared paint/geometry/focus/tokens land in the parent DS so every consuming app inherits the fix. Hosts keep recipes and Pages-safe holds. Pin/export order stays [[cds-host-consume-order]]. Triggers: `parent vs host`, `cds or proto`, `consuming app`, `host overlay`, `shared component defect`. (2026-09-22)
- [[ds-ops-governance-notes]] — Workspace-side companion to the [[design-system-ops]] hub (the pack's own `knowledge-notes/` are vendored, unindexed, and overwritten on sync): ops is a separate discipline from authoring and audits **against** [[ds-advisor]] strategy rather than setting it; tier leakage is the recurring token defect and is a governance failure first; one theme control point beats two and contrast governs the semantic layer (both from prior validated token work); drift is a system-gap signal, not a compliance list; component deprecation follows the anti-zombie rule (replacement + codemod + comms, then removal). Triggers: `design system ops`, `ds ops`, `token audit`, `tier leakage`, `token drift`, `drift check`, `deprecation`, `governance review`, `design system governance`, `adoption report`, `system health` (2026-08-03)
- [[radix-derived-color-system]] — Validated color-foundation architecture: Radix as source of truth + Tailwind-name compat aliases (nearest-OKLCh-L) + APCA-as-governance (not primitive mutation) + brand-aware semantic hue collision-avoidance + accent(hover, step 4) vs selected(active, step 5). CSS `contrast-color()` is a pairing helper onto semantic text tokens, not an APCA substitute. Triggers: `color tokens`, `palette generator`, `radix colors`, `apca`, `accent token`, `brand color`, `semantic colors`, `contrast-color` (2026-06-02; CSS pairing 2026-09-02)
- [[cds-to-radix-color-map]] — Retired CDS→Radix bridge catalog (42 tokens removed from Figma SaaS PLM library 2026-07-31): full Light/Dark primitive map for the centric-ui migration; hue remaps black/gray→Zinc, pink→Rose, purple→Violet. Triggers: `cds colors`, `cds-blue`, `cds palette`, `radix map`, `centric-ui migration`, `remove cds tokens` (2026-07-31)
- [[cross-surface-token-parity]] — MATCH vs ALIGNED vs DEVIATE when comparing Figma↔code: lean ALIGNED when intent + visual outcome match but surface cannot host the other’s representation (e.g. `radius/full` 9999 vs `rounded-full`); never say “no parity” when intent is shared. Triggers: `token parity`, `aligned`, `radius/full`, `figma vs code`, `implementation disparity` (2026-08-05)
- [[token-spec-page]] — Token Spec page in Figma DS (`o6o1ZuGHxDow2vHLuYXT6X`): Figma ↔ centric-ui ↔ prototype comparison. Triggers: `token spec`, `density align`, `radius/full` (2026-08-05)
- [[density-radius-xxs-alignment]] — Density-modeled Radii + `radius/xxs` across Figma / Proto / centric-ui. Triggers: `radius/xxs`, `density ladder`, `data-density` (2026-08-05)
- [[density-vertical-rhythm-audit]] — Vertical rhythm / adornment + Icon Button density follow-ups across Figma↔code. Triggers: `vertical rhythm`, `icon button density`, `adornment`, `density audit` (2026-08-06)
- [[density-row-height-explained]] — What dashboard `ROW_HEIGHT` controls (edit units vs view hug). Triggers: `ROW_HEIGHT`, `dashboard grid`, `react-grid-layout` (2026-08-05)
- [[density-dashboard-content-sized-grid]] — Dual layout: view hug/fill vs edit RGL; `sizing` registry. Triggers: `dashboard layout`, `content sized`, `hug`, `fill widget` (2026-08-05)
- [[density-dashboard-page-inset-vertical]] — Grid owns page inset; KPI hug; Tailwind scanner gotcha. Triggers: `page-inset`, `dashboard inset`, `card-pad` (2026-08-05)
- [[density-dashboard-visual-review]] — Home dashboard density visual review + wiring status. Triggers: `dashboard density`, `KPI strip`, `density review` (2026-08-05)
- [[figma-to-code-parity-plan-2026-08-06]] — Session plan inventory: Figma SaaS PLM DS → Proto + centric-ui parity (P0 radius SSOT, overlays, focus twins); employer PRs only. Triggers: `figma to code`, `parity plan`, `control radius`, `rounded-md`, `radius-focus` (2026-08-06)
- [[figma-shadow-modes]] — Single `shadow/cds-drop` style + elevation modes Drop 1/2/3. Triggers: `shadow modes`, `cds-drop`, `elevation`, `figma shadow` (2026-08-05)
- [[brand-text-vs-fill]] — `action/primary` is fill (Blue/10); brand text on washes uses Blue/11 `primary-text`; brand-soft is Blue/3+11+6 (`primary-soft*`), never info-soft. Triggers: `primary-text`, `primary-soft`, `brand text`, `selected foreground`, `Blue/11`, `info-soft` (2026-08-05; soft pair 2026-09-15)
- [[interaction-state-semantics]] — Interaction = alpha overlay over base fill, never substitute background. Triggers: `state layer`, `interaction overlay`, `hover pressed`, `interaction tokens` (2026-07-31)
- [[stickersheet-inventory-plan]] — Figma Stickersheet Inventory page (shipped review matrix). Triggers: `stickersheet`, `inventory matrix`, `component matrix` (2026-08-05)
- [[centric-ui-density-adoption]] — Where centric-ui consumes `--density-*`; remaining hardcoded `h-8` gaps. Triggers: `density not working`, `h-8`, `control height`, `homepage density` (2026-08-05)
- [[figma-component-token-axes]] — Instance vs context component-token method: Density/Color = shell context; Size/Variant/Position = instance; Color collections alias semantics and publish WEB `var(--sem-*)`; code stays on Tailwind semantic utilities. Triggers: `component tokens`, `density mode`, `instance axis`, `context axis`, `sidebar surface`, `control-font-size`, `mode stacking`, `WEB syntax` (2026-07-31; Color WEB map 2026-09-15)
- [[figma-ds-surface-authoring]] — Durable DS Figma authoring conventions for any surface/overlay: build-from-real-components (props-first), surface/overlay construction rules (edge-to-edge separators, spacing-token binding, absolute-positioned in-parent popovers, icon↔label match, no double padding, slot fill/hug, mode-first variants, total tokenization, floating-element constraints, **catalog section ownership + no AABB overlap**), and code→Figma transliteration judgment calls (focus uses `ring` token; otherwise code-faithful). Triggers: `figma authoring`, `surface authoring`, `overlay`, `popover`, `separator`, `mode-first`, `tokenize`, `transliteration`, `focus token`, `section overlap`, `catalog section` (2026-06-30; AABB rule 2026-09-16)
- [[figma-opacity-variables]] — Figma UI (2026-09-03): number vars on color-variable + fill opacity without detaching. MCP/`use_figma`/`node.set` bind **layer** opacity only (FLOAT 0–100); paint/color-var writes still reject (re-probed 2026-09-17). Triggers: `opacity variable`, `figma opacity`, `bind opacity`, `MCP opacity`. (2026-09-17)
- [[enterprise-charting-and-dataviz]] — Enterprise charting stack (ECharts / Carbon / Recharts) vs CDS table-chart; encoding and token constraints. Triggers: `charting`, `echarts`, `recharts`, `dataviz`. (2026-09-09)
- [[enterprise-charting-pm-brief]] — Plain briefing for product management on charts in the product. Triggers: `charting pm brief`. (2026-09-10)
- [[nathan-curtis-ds-ops-substack]] — Curtis Substack ops notes absorbed: **configuration collapse** (slots over prop sprawl; make common configurable / uncommon composable; AI-ready composition grammar), specs-on-command as testimony→contract, many-core-library sync, fallacy of federated DS, Figma component testing as design Proofboard, DS Generations governance. Triggers: `configuration collapse`, `figma slots`, `prop sprawl`, `composability`, `specs on command`, `federated design system`, `many core libraries`, `testing figma components`, `design system generations`, `nathan curtis` (2026-08-07)
- [[centric-plm-design-system]] — DS strategy, token architecture, data table scope, Ark UI decision, cross-framework approach; 2026-07-31 density modes + semantic slash naming + color category folders (2026-04-28)
- [[davinci-ds-boilerplate]] — Portfolio demo-rebuild standards migrated from machine-local memory (FX-15): the nexus pnpm+turbo four-surface boilerplate, dogfooding definition (chrome literally uses the DS), the Radix 12-step token CONTRACT (text 11/12 · solids 9/10 · borders 6–8 + CI guards), and the canonical DS docs IA (Carbon/Atlassian/Polaris-grade). Triggers: `davinci`, `nexus`, `dogfooding`, `portfolio demo`, `docs ia`, `12-step contract` (2026-07-09)
- [[visual-first-documentation]] — **A definition without a picture is unfinished.** Live instance → live composition of primitives → labeled schematic (if it does not ship) → Storybook as jump-out. If it ships and the docs app is missing a peer, add the peer — do not caption the gap. Text captions the picture; tables of names are not the example. No host-product screenshots in system docs. Triggers: `documentation`, `live example`, `docs dump`, `pattern docs`, `visual documentation`, `Storybook embed` (2026-09-14; host-gap 2026-09-15)
- [[canonical-documentation]] — **Canonical pages describe the system, never the docs site's debt.** No changelog notes, "for now" schematics, or "Storybook has the real one because we couldn't." Code tab may list consume facts (optional peer, height). Skill: [[canonical-docs-voice]]. Triggers: `changelog note`, `canonical documentation`, `because the docs app`, `schematic here`, `docs voice` (2026-09-15)
- [[plain-language]] — **Prefer the ordinary word.** Do not coin sideways terms that are more precise on paper and foggier in use; name the thing by what the user does, then by the component they can open. Specialist vocabulary stays for tokens, variants, states, anatomy, slots. Applies in chat, docs, specs, canvas labels, engineering and PM writing. Triggers: `chassis`, `lateral terminology`, `plain language`, `jargon`, `parent component`, `usage versus variant` (2026-09-14)
- [[centricsymbols-icon-font]] — Variable icon font architecture: 4 axes, Figma authoring constraints, pipeline, COLRv1 (2026-04-28)
- [[enterprise-saas-design-patterns]] — **Master operational reference**: 28-pattern catalog from 2026-05-12 Mobbin audit; cross-pattern primitive spine (StatusPill, Drawer, TypedFieldEditor, ActivityItem, RelationChip, Stepper, PropertiesRail, KeyboardShortcutMenu + 7 AI-provenance primitives); when-to-build-X decision routing; token vocabulary (density / status / cell-state / drawer-size); AI provenance discipline checklist; anti-patterns. **Auto-load on enterprise SaaS layout work** — triggers: `data table`, `record detail`, `bulk edit`, `saved view`, `filter chip`, `drawer`, `side panel`, `approval workflow`, `audit trail`, `csv import`, `inbox`, `notifications`, `cell anatomy`, `record lifecycle`, `diff view`, `record comparison`, `tree table`, `BOM`, `ai summary`, `provenance`, `enterprise saas`, `PLM layout`. (2026-05-12)
- [[meridian-ds-prototype]] — Personal DS prototype: token system, component state coverage, ARIA audit, WCAG results (2026-04-28)

## Engineering

- [[intent-app-coordination]] — Intent (intentapp.dev / intent-hq releases): adopt living spec, worktree isolation, independent verifier; refuse vendor lock. Runner: `09-tools/intent-run.py`. Triggers: `living spec`, `intentapp`, `intent coordination`, `coordinate agents`, `agent orchestration`, `isolated worktree` (2026-09-04)
- [[agent-work-queue-boundaries]] — **Seven boundary constraints for a multi-tenant agent task queue**, all found by testing rather than reasoning: **connection isolation ≠ runner isolation** (per-tenant credentials scope reads; user-scope registration still binds every server to every session, so unattended runners need process-level scoping); **identity cannot be verified read-only on an empty tenant** (Linear 2026-07: slug only on object URLs → first-write gate; 2026-09-02: `get_workspace` returns the slug on a read, first-write remains the fallback; distinctness _is_ provable read-only where user records are per-tenant); **never rely on a create-default** (renaming a default silently moved it, orphaning new issues outside the claimable pool); **entity ids collide across tenants** (qualify `tenant:ID` from day one — the risk is mis-resolution, not confusion); **a movement-only queue makes deletion structurally impossible** (pointers into a substance store enforce "don't delete the original" architecturally); **cloud schedulers cannot reach locally-authenticated tools** (scheduling and isolation pull against each other); and **untrusted input + autonomy = remote execution** (safe apart, dangerous together, easy to reach one reasonable decision at a time). Triggers: `agent queue`, `open engine`, `work queue`, `multi-tenant`, `lane isolation`, `mcp scope`, `claim lock`, `movement only`, `prompt injection`, `unattended agent`, `scheduled agent`, `tenant identity`, `id collision` (2026-07-30)
- [[contracts-first-delivery]] — The vault evidence behind [[14-engineering-operating-model]]'s contracts-first conviction; generalizes [[silent-degradation-in-fenced-layers]] + [[centric-ui-local-against-cloud-dev]]: every boundary has a contract, and the expensive bugs were real-but-unwritten contracts where a violation looked like normal behavior. Write it in plain english first (inputs, output shapes, **empty vs. broken**, ordering); give every fenced layer a side channel for *why* and classify fatal vs. per-item before collapsing; trust the code's validation order over the docs and read error text narrowly; every governing param is in the cache key; prove failure paths at the boundary via a Proofboard. Diagnose before hardening. Triggers: `contract`, `api contract`, `error handling`, `fail open`, `fallback`, `observability`, `cache key`, `stale cache`, `validation order`, `proofboard`, `boundary` (2026-08-03)
- [[centric-plm-codebase]] — Confirmed Centric 8 tech stack, dual frontend (Dojo/React), analysis project structure (2026-04-28)
- [[centric-plm-frontend-stack]] — CDS file: link quirks: TS2786 React types mismatch, tsconfig paths fix, circular interface fix, full DataTable API surface map. Triggers: `ts2786`, `tsconfig paths`, `cds link`, `datatable api` (2026-05-05)
- [[centric-vms-frontend-stack]] — Quirks discovered shipping centric-ui Storybook + ds-docs Fumadocs site: ESLint 10/Next compat, Fumadocs Tabs uncontrolled by design, Tailwind 4 `@theme inline` baking, useSyncExternalStore pattern, Storybook iframe theme sync. Triggers: `fumadocs`, `storybook build`, `eslint 10` (2026-04-29)
- [[centric-ui-local-against-cloud-dev]] — Running centric-ui locally against the CLOUD dev backend. **Key rule: a 401 from a BE call is always the `x-api-key`, never the token or tenant** (validation order is api-key→401, tenant→403, Bearer passthrough) — and "Unauthorized API path" means the key isn't authorized for that path, NOT that the path is wrong. Three traps: the documented API key is the local-compose one (read the real one from the deployed app's own request headers); `resolveServiceBaseUrl` hard-returns "" in DEV so the Vite proxy table alone decides routing, and record/schema-registry/workflow are still localhost-only; Keycloak's `react` client accepts only `localhost:3000` while vite defaults to 8082 and the example file says 5173. Also: workflow-service has no cloud host (000, not 401). Triggers: `cloud dev`, `env.local`, `x-api-key`, `Unauthorized API path`, `redirect_uri`, `vite proxy`, `ECONNREFUSED 9011`, `9012`, `centric-ui local`, `sidebar empty`, `keycloak realm VMS` (2026-07-20)
- [[abstraction-hides-its-dependencies]] — A host that consumes a design system does not declare widget libraries it does not import. Peers are shared singletons (React); a second copy of a date picker is not a correctness bug. Barrel `export *` is how an unused widget enters the consumer bundle. An unmet peer is not a work order. Triggers: `peer dependency`, `unmet peer`, `implementation dependency`, `widget peer` (2026-09-21)
- [[cds-host-consume-order]] — Pages CI vendors cds **`main`**. Overlay can export paths `main` does not. `cds-exports-check` gates subpaths, not named exports. Vite 8 / Rolldown can still miss package export maps (phosphor CSR). `lint-ds` ratchets new files. Sibling `CDS_ROOT` outside proto ≠ Pages `_cds`. Order: cds PR onto `main` → then proto consume. Triggers: `pages build`, `cds then proto`, `overlay vs main`, `consume cds`, `re-export`, `phosphor csr`, `lint-ds ratchet` (2026-09-17)
- [[cds-host-buttongroup-realign]] — Host ButtonGroup / split-CTA deltas for cui federalization. `ButtonGroup` is a join shell; child `Button` `variant` paints. Live cui has no ButtonGroup (header is `variant="dark"`). Older cui-main used a dark split CTA + `rounded-md` overlay, not outline/primary. Proto's joined control is `SplitActionButton`, not the unused local `button-group.tsx`. Do not copy CDS Default filled primary into toolbars. Triggers: `button group`, `buttongroup`, `split action`, `cui federalization`, `ViewHeaderActions` (2026-09-18)
- [[claude-code-hooks-contract]] — Claude Code hook payload contract: `hookEventName` must equal the event or the payload is SILENTLY dropped; PreToolUse deny-with-reason as a deterministic model-injection point; SessionStart `source` (compact/resume) re-injection; regex matchers for MCP tools. Triggers: `hookEventName`, `pretooluse`, `userpromptsubmit`, `sessionstart hook`, `additionalcontext`, `hook payload`, `hooks contract`, `hook didn't fire`, `claude hooks`, `hook dispatcher` (2026-07-09)
- [[claude-code-mcp-scope]] — `claude mcp add` defaults to local (cwd-scoped) — use `--scope user` for global. Three scopes explained; diagnosis recipe; OAuth-on-first-call for HTTP transports; restart-required behavior. Triggers: `mcp scope`, `claude mcp add`, `scope user` (2026-05-12)
- [[claude-code-skills-vs-slash-commands]] — Skills are model-invocable everywhere but only locally-installed ones show in the `/` menu; cloud/Cowork bundles (`anthropic-skills:*`) don't. How to wrap a skills dir as a local plugin/marketplace so it surfaces as `/<plugin>:<skill>` (plugin.json has no skills-path field → must copy under `skills/`; keep outside Drive; restart required). Triggers: `slash command`, `skills vs commands`, `skill menu` (2026-06-02)
- [[figma-plugin-patterns]] — Hard-won Figma plugin patterns from the figma-repo-sync-plugin Bundles 5–7: layoutSizingHorizontal/Vertical=FILL beats layoutAlign=STRETCH, three-tier upsert for persistent artifacts, Tailwind /N opacity parsing requires color-class gate, multiply opacity on paint not variable, createFrame()'s 100×100 seed requires resize(1,1) kickstart — **plus migrated Figma-API gotchas (§15–17): MS-icon min/max-width (rescale not resize), SectionNode parent-relative coords, use_figma MCP is headless** (2026-05-11, ext. 2026-06-30). Triggers: `figma plugin`, `layoutsizinghorizontal`, `plugin api`
- [[figma-cli-authoring]] — Figma-CLI (Yolo/CDP) authoring: connecting (the "Full Disk Access" error is a red herring — Bash sandbox is the real blocker), branch targeting via `FIGMA_TARGET_TITLE` (+ the speed-daemon bypass + the flaky-render trap), self-invoking async IIFE for cross-path eval, verify/screenshot fallbacks, parallelize-analysis/serialize-mutation, creating SLOTs without `figma.createSlot`, creating variables, depth-aware section relayout, exclude SECTION chrome from token sweeps, glyph swap + mode-first application, probe-before-fixing render-ambiguous findings. Triggers: `figma-cli`, `figma cli`, `CDP`, `branch targeting`, `yolo`, `figma daemon`, `figma eval` (2026-06-30)
- [[figma-tailwind-token-pipeline]] — Cross-project mechanics from the generator-driven Radix→Figma migration: (1) Tailwind v4 unlayered `:root` overrides `@layer theme` so a palette's VALUES swap wholesale without touching utilities (verify via `@tailwindcss/node` compile); (2) Figma `Variable.consumers` returns 0 even for bound vars → audit usage via alias-refs / node-walk, not consumers; (3) CSS = full dev compat, Figma = design-surface subset only (shades/alpha CSS-only; shade→step binding); (4) one theme-control point (flat primitives + themed semantics) beats two; (5) self-healing migration — retire-by-name, sweepUnseeded, split-brain resolve, O(n) index to avoid the 1700-var O(n²) hang. Triggers: `tailwind`, `design tokens`, `figma variables`, `palette`, `css cascade`, `token pipeline`, `Variable.consumers`, `@theme` (2026-06-04)
- [[shadcn-lint-token-tiers]] — `@shadcn/lint` as an independent product-repo DS linter; stock `no-raw-colors` false-greens `bg-cds-blue-500` (CDS `@theme inline`); overlay `ds-lint/no-tier-leakage` is required in `eslint.ds.config.mjs`. Overlay-first. Not vault CI. Sibling of `eslint-off-system`. Triggers: `shadcn lint`, `lint:ds`, `@shadcn/lint`, `no-restyle`, `no-raw-colors`, `no-tier-leakage`, `ds-lint` (2026-09-15)
- [[figma-variable-state-representation]] — Figma variable resolution mechanics + the CVA variant×state→Figma pattern (Bundles 11.3.49–70): modes are a vertical slice (one axis physical when variant+state collide); opacity can't be mode-driven → normalized state-layer (Decision B); `resolvedVariableModes`/`resolveForConsumer` are the authoritative resolver (pin-heuristics give false positives); Selection Colors `(?)` ≠ broken binding (multi-value cosmetic); the black-default resolver trap; grouped `<slot>/<state>` naming + explicit `default` affordance; single-source-of-truth + palette-generator migration seam. Triggers: `variable resolution`, `cva variant`, `variant state mapping` (2026-05-23)
- [[nexus-monorepo-playbook]] — Solved gotchas for the DS+docs+Pages monorepo (migrated FX-15): pnpm strictness, Docusaurus dogfood pipeline (Infima specificity trap), shadcn shadow flattening, preflight-off button bevel, Storybook manager theming, GitHub Pages multi-surface deploy, TS-strict coercions, repo IP hygiene. Triggers: `docusaurus`, `storybook theming`, `github pages`, `preflight`, `swizzle`, `pnpm` (2026-07-09)
- [[silent-degradation-in-fenced-layers]] — A fence that degrades to `None`/absent/default erases the difference between "genuinely nothing" and "the mechanism failed" — a broken layer looks identical to a healthy-but-empty one, no error anywhere. Found 3× in one MediaSentinel session: swallowed timeout = no data, `except Exception: return None` making credit-exhaustion = "no guest found" (94 silent Nones), cache key omitting a governing param = stale hit; plus a progress counter that measured the cheap phase. Rules: give fenced layers a side channel for *why* (counter/last_error/one-time stderr); classify fatal vs per-item before collapsing; a cache value's every governing param is in the key or invalidation is explicit; monitor the real output artifact, never a counter you didn't verify. Triggers: `silent failure`, `except Exception`, `return None`, `graceful degradation`, `advisory layer`, `cache key`, `stale cache`, `swallowed error`, `observability`, `progress counter` (2026-07-22)
- [[pokemon-tvdb-dvd-vs-aired]] — ColdFusion Pokémon pack codes are production numbers, not TVDB seasons. Map via folder context + TheTVDB DVD. Plex/Emby default aired now maps 1997-show S20 to Horizons. Triggers: `pokemon`, `tvdb dvd`, `horizons`, `coldfusion pack`, `production number` (2026-09-03)
- [[web-3d-runtime-stack]] — vgpu default plus optional extensions (chisel, maige-3d, Godot/Unity/Unreal MCP). Triggers: `vgpu`, `webgpu`, `wgsl`, `web 3d`, `gltf-transform`, `chisel`, `mcp-webgpu`, `godot mcp`, `unity mcp`, `agentbridge` (2026-09-04)

## Data Science

- [[experiment-validity-baseline]] — The bar for any quantitative claim here, under [[15-analysis-operating-model]]: a **pre-committed decision rule** (if X do A, if Y do B; if both branches match, don't run it) plus six pre-registered fields, then the four validity questions in order (construct → internal → external → statistical, since a failure high up moots the rest). Recurring traps: peeking, uncounted multiplicity across every metric and slice inspected, unstated denominators, silent upstream degradation destroying construct validity ([[silent-degradation-in-fenced-layers]]), and reporting a volatile count as a stable score ([[adversarial-verify-label-volatility]]). State the evidence tier out loud; ship a Proofboard. Triggers: `experiment validity`, `experiment`, `a/b test`, `ab test`, `statistical significance`, `p-value`, `power analysis`, `causal`, `validity`, `pre-register metric`, `guardrail metric`, `stopping rule`, `peeking`, `multiple comparisons`, `metric definition` (2026-08-03)

## Accessibility

- [[a11y-measurement-vs-judgment]] — Contrast via visual-qa-toolkit; structural WCAG via a11y-audit-toolkit; judgment via a11y-* . Triggers: `a11y audit`, `axe vs judgment` (2026-08-03)
- [[accessibility-beyond-wcag-baseline]] — The workspace bar for an accessibility *claim* (the depth behind [[a11y-measurement-vs-judgment]]'s routing): WCAG is a compliance floor and an automated scan covers a minority of real barriers, so the four-part manual baseline (keyboard-only path, screen reader, zoom/reflow to 200%, motion/timing) is mandatory; contrast policy governs the **semantic** layer and never mutates the primitive ramp (builds on [[radix-derived-color-system]]); framework #10 perception integrity applies to a11y review (focus rings and 1px borders vanish under downsampling); dense enterprise data tables are the hard case (header association, grid vs. tab navigation, color-only status). Triggers: `accessibility`, `a11y`, `wcag`, `contrast`, `focus ring`, `keyboard navigation`, `screen reader`, `zoom reflow`, `color only`, `data table accessibility`, `axe scan` (2026-08-03)

## Security

- [[security-pipeline-baseline]] — Threat→build→scan→monitor (#16); be-security-posture is depth not the gate. Triggers: `threat model`, `security done-gate` (2026-08-03)
- [[threat-model-before-controls]] — The reasoning behind [[security-pipeline-baseline]]'s first stage: write the **asset / adversary / accepted-risk contract** before discussing any control, since a control with no named threat can never be evaluated, tuned, or removed. Trust boundaries are every place data changes authority (including cache, config, dependency, tenant-to-tenant). Validated auth-boundary lesson from [[centric-ui-local-against-cloud-dev]]: rejection **order** determines the status code you see, and error text is a narrow fact not a hint. Fail closed and make the rejection observable, or a broad `except` silently converts a deny into an allow ([[silent-degradation-in-fenced-layers]]). An accepted risk that is not written down is a forgotten risk. Triggers: `threat model`, `stride`, `trust boundary`, `attack surface`, `asset adversary`, `accepted risk`, `fail closed`, `fail open`, `authorization check`, `secrets in git`, `multi-tenancy` (2026-08-03)

## Career

- [[career-skills-routing]] — Workspace wrappers vs ~/.agents skill depth. Triggers: `job search`, `career ops` (2026-08-03)
- [[positioning-over-volume]] — What to do once [[career-skills-routing]] has told you where the depth lives. Order is **strategy → materials → pipeline** and does not reverse: automation multiplies whatever it is given, so applied first it industrializes an undecided position. [[role-and-context]] is the only evidence base (reframing allowed, invention not); an automated A-F fit score ranks for attention but does not decide, and a stale profile/CV input yields confident garbage. `personal-solo` separation: company-named research and drafts stay out of tracked files. Honest limit: doctrine at seed time, no outcome data yet. Triggers: `job search`, `career transition`, `positioning`, `tailor resume`, `cover letter`, `career-ops`, `offer scoring`, `interview prep` (2026-08-03)

## Game Dev

- [[legion-architecture]] — Legion tech stack, V1 scope, 3D asset context, skill routing, open questions (2026-04-28)
- [[threejs-galaxy-visualization]] — Five hard-won Three.js patterns from the 2026-05-11 Legion galaxy pass: userData.type for selection, per-layer pattern uniqueness for stacked-shader volume, dedicated dust planes for true occlusion, opacity ramps for seamless zoom transitions, per-object camera framing scale. Triggers: `threejs galaxy`, `galaxy visualization`, `per-layer raycast` (2026-05-11)
- [[legion-galaxy-playbook]] — Prescriptive build recipe for the Legion galaxy: the skill-load chain + ordered sequence (scale architecture → star field → volumetric disc/nebulae → dust occlusion → HDR/ACES pipeline → hero bodies → flythrough camera) + 60fps perf budget. Wires the new astro/VFX/scale-traversal skills; defers gotchas to [[threejs-galaxy-visualization]]. Triggers: `galaxy`, `nebula`, `star field`, `flythrough`, `volumetric`, `scale traversal`, `legion galaxy` (2026-06-16)
- [[legion-hero-body-rendering-research]] — **Master research dossier** (adversarially-verified, 16-agent workflow) for SpaceEngine-class close-zoom fidelity at 90 fps in Three.js+WebGPU: recommended architecture per hero body (planet surface, star photosphere, black hole), the honest verdicts (real budget ~8–9 ms not 11.1; black hole = scripted hero moment not free-fly; WebGPU has no mesh/tessellation/VRS/fp64), a unified 90 fps frame-budget table, cited primary sources per pillar, and the four-skill blueprint. Authoring source for [[planetary-terrain-lod]] / [[atmospheric-scattering-and-clouds]] / [[stellar-and-relativistic-hero-bodies]] / [[realtime-render-performance]]. Triggers: `90fps`, `planetary rendering`, `atmospheric scattering`, `volumetric clouds`, `star surface`, `black hole`, `gravitational lensing`, `TAAU`, `WebGPU performance`, `space engine`, `hero body` (2026-07-22)
- [[legion-planet-surface-rendering]] — Legion-specific hard-won planet-shader patterns (`src/render/planet/`): dual-height terrain (baked atlas for geometry + continuous analytic macro for thresholds), Whittaker biomes with lapse-rate, the **hex-artifact fix** (decouple biome color from baked-height seams so the ±0.08 treeline smoothstep stops flipping at quadtree leaf edges), latitude snow/ice, the open flashing-storm bug, and the GLSL reserved-word (`active`) gotcha. Triggers: `planet shader`, `biome`, `treeline`, `hex artifact`, `snow`, `ice cap`, `storm`, `whittaker`, `lapse rate`, `glsl reserved` (2026-07-22)
- [[legion-analytic-gradient-normals]] — Close-zoom lead salvaged from the Sandahl procedural-planet PDF: derive terrain normals from the noise's **analytic gradient** (Gustavson psrdnoise) instead of 3-tap finite differences — `g⊥ = g−(g·n)n`, `N = normalize(n−g⊥)` — and render the **highest octaves as analytic bump, not displaced geometry** (spends ALU, not vertices/bandwidth; matches the close-zoom regime). Corrects the Michelic 2018 reference (persistent *projected grid*, NOT cube-sphere quadtree → do not adopt over Legion's CDLOD) + wrapped-diffuse SSS note. Triggers: `analytic normals`, `gradient normals`, `bump mapping`, `psrdnoise`, `high octave`, `close zoom`, `terrain normal`, `procedural planet paper` (2026-07-23)

## Research

_Raw research syntheses live in `research/` (double-nesting flattened 2026-07-23)._

- [[glsl-shader-programming]] — GLSL shader programming reference for WebGL/Three.js (2026-06)
- [[threejs-materials-deep-dive]] — Three.js PBR materials system complete reference (2026-06)
- [[threejs-postprocessing-vfx]] — Three.js post-processing, particles, VFX pipelines (2026-06)
- [[threejs-showcases-techniques]] — Award-winning Three.js showcases + production techniques (2026-06)
- [[webgpu-tsl-deep-dive]] — WebGPU, Three.js TSL & WebGPURenderer deep dive (2026-06)
- [[agentic-error-correction-foundations]] — **Independent measurement is the correction primitive.** Same-model self-critique is not error correction. Field map of loop types (Self-Refine / Reflexion / CRITIC / sampling / debate) narrowed onto this workspace's existing gates; workstream detector registry; honesty bound that unexpected results cannot be fully removed, only made detectable, non-silent, and non-repeatable. Mechanical close 2026-08-26: #06 detector check, embedded prove/validate, negative fixtures, observable routing skips, `/optimize` seven-surface ECC. Triggers: `error correction`, `verification loop`, `agent reliability`, `generate-verify`, `self-correct`, `self-refine`, `reflexion`, `unexpected results`, `done-claim`, `independent detector` (2026-08-26)
- [[perception-critique-stack]] — **The field is a metric ladder, not a better SSIM.** Pixel/structure (our prove engine) → psychovisual HVS maps (NVIDIA FLIP, Butteraugli) → mid-level learned (DreamSim) → no-reference aesthetic (Q-Align) → geometric/3D-native (VGGT, glTF auditor; Chamfer and CLIP-IQA lie) → process critics (VisCritic on top of `vqa interact`) → simulation/feel/balance (input-to-photon, CoTracker jerk, AutoSim, MCTS). VLMs fail first-fixation hierarchy; GPU goldens are not byte-stable; Literal UI stays on the pixel rung. Course corrections 1–12 plus a no-weights first slice. Triggers: `DreamSim`, `FLIP metric`, `VGGT`, `3D QA`, `mesh quality`, `game feel`, `input latency`, `saliency`, `VisCritic`, `glTF validator`, `playtest agent`, `novel view synthesis`, `perceptual metric`, `machine vision QA` (2026-08-28)

## Cross-Domain

- [[tool-adapter-discovery]] — Native-filename thin adapters (`GEMINI.md`, Copilot instructions, `WARP.md`, Aider `CONVENTIONS.md`, Windsurf rules, web-session paste pack) point at `AGENTS.md`. Never symlink the contract; never add `.cursorrules` / `.windsurfrules` / `.clinerules`. Triggers: `tool adapter`, `gemini.md`, `copilot instructions`, `web session`, `WARP.md`. (2026-09-11)
- [[agent-load-miss-review]] — Hub-gated adversarial review of what a cold LLM (Claude/GPT/Grok/Cursor) never loads vs what it over-loads. Layer 0 is a hook plus a hope; silent hubs; AGENTS read-order vs token diet; #18 Frost is routed not contracted. Map-only — numbered recs in `07-projects/19-workspace-brain/reports/harness-map_v2.0_2026-09-11.md`. Triggers: `agent miss`, `load miss`, `cold agent`, `what would an agent miss`, `token diet`, `silent hub`. (2026-09-11)
- [[process-rigor-gaps]] — Doctrine exists; it does not attach. Recs **R1–R16** applied 2026-09-11 with load-miss 1–15. First-wave attach: `skill-loadset.py` + `close-out-dispatch.py` + Layer 0 schema + secret scan. Close-out: agent runs capture/assess/correct; missing detector → mint + independent push; page Sean only if self-critique or mint still fails. Self-improve: per-session correct/heal/improve for the rest of the vault. Triggers: `process rigor`, `close-out`, `self-police`, `human visual qa`, `prove-gate`, `llm qa`, `capability mint`. (2026-09-11)
- [[self-improving-workspace]] — Correct (independent detectors) and heal (CI/rebuild) already existed; improve-through-normal-use did not, and `/optimize` `/health` were Claude-only + sign-off-gated. Home: [[self-improve]]. Triggers: `self-improve`, `self-heal`, `self-correcting`, `connective tissue`, `transferable process`. (2026-09-11)
- [[cursor-employer-repo-skill-routing]] — When Cursor opens an employer repo, Brain is not folder 1; inject Layer 0 via `beforeSubmitPrompt` / `prompt_route.py` or vendor Figma skills win. Triggers: `beforeSubmitPrompt`, `employer repo`. (2026-09-09)
- [[figma-source-audit-patterns]] — Five source shapes; state-coverage taxonomy; recurring gaps observed in the centric-ui Figma library (sizing, variants, composition, properties, variables, indicators); per-component recommendations table. Triggers: `source audit`, `state coverage`, `figma library audit` (2026-05-08)
- [[figma-component-composition-from-react]] — Compound generation two-layer model (outer story-driven, inner JSX-anatomy); Figma INSTANCE constraints; variant/independent/INSTANCE_SWAP decision matrix; shadcn conventions. Triggers: `compound component`, `jsx anatomy`, `nested instance`, `component composition` (2026-05-07)
- [[workspace-infrastructure]] — Git-native workspace infrastructure: hook dispatcher (5 events, three-source tiered trigger routing), bootstrap-v2 machine layer (dist shims + doctor + beacon, per-machine installs), multi-machine topology, multi-identity GitHub; headless Claude Code scoping (`--strict-mcp-config` isolates, `--allowed-tools` does NOT restrict built-ins, mcp-remote cold-start silent failure); Drive-era learnings quarantined as historical (2026-07-30)
- [[nate-jones-harness-enrichments]] — Nate B. Jones posts/guides absorbed into harness ops (not re-implemented): skill one-job test, ownership cards, judge layer, control checklist, maintenance loop (job/diet/memory/tools/reach/proof/value), token/preload diet, bakeoff discipline, one-minute work-shaping, Open Stack vs file-first continuity, slop/read-with-care, steer vs dispatch surface posture, issue-trackers-as-infra. Triggers: `harness enrichment`, `skill one-job`, `agent ownership`, `judge layer`, `maintenance loop`, `token diet`, `preload`, `one-minute test`, `open stack`, `ai slop`, `steer vs dispatch`, `nate jones` (2026-08-07)
- [[zero-vector-design-methodology]] — Erika Flowers' Zero-Vector Design synthesized: principles + 8-phase pipeline, Investiture doctrine/audit skill chain (prompt-only, drifts), Open Vector curriculum (6 levels/40 lessons), Labrador context receipt; dogfood evidence ("done but not closed", llms.txt drift); clean-room transferable patterns (project intent declaration, spec-lint + drift, finding ledger + closure, phase packets, role charters, research records, entry-point parity) → harness plan. No LICENSE upstream. Triggers: `zero vector`, `zero-vector`, `investiture`, `open vector`, `vector.md`, `erika flowers`, `finding ledger`, `phase packet`, `done but not closed` (2026-09-22)
- [[multi-session-workspace-resilience]] — Conflict-free multi-session/device/surface workspace: per-session fragments + union-merge logs + idempotent compaction + scoped commit + safe push-retry (autostash pinned OFF); token frugality via bounded/archived logs (O(1) read cost). Diagnostic lessons: autostash=false is the safety pin; rebase re-hashing is cosmetic not lossy; diagnose before hardening; **the git index is SHARED — `git add <mine>` + `git commit` takes the whole index, so use `git commit -- <paths>`**, and scoped-commit covers neither Bash-written files nor mid-session manual commits. Triggers: `multi-session`, `concurrent session`, `cross-machine sync`, `session-log`, `git pull rebase`, `autostash`, `token frugality`, `session fragment`, `compaction`, `workspace resilience`, `shared index`, `git index`, `scoped commit`, `concurrent agents`, `same working tree` (2026-07-23)
- [[workflow-patterns]] — Session-start card (`session-status.py` on every surface); stale-content review, 3-bucket pending, session-end habits, trigger-loading; + self-contained-tool-copies-go-stale (fresh-init testing), baseline-diff integrity gate, consent+quarantine+scan ingestion (2026-09-11)
- [[adversarial-verify-label-volatility]] — In a map→adversarially-verify multi-agent audit, the per-unit CONFIRMED/ADJUSTED/REFUTED label is sampling noise, not signal: two passes over identical mapped units swung 8/19/6 → 17/16/0 while every unit's rung + difficulty stayed identical. Report the stable mapped verdict ("two passes agreed on rung+difficulty; flags were refinements not reversals"), never a confirmed/adjusted count; aggregate k≥3 or gate on the correction diff for a durable pass/fail; independently hand-check load-bearing claims. Triggers: `adversarial verify`, `multi-agent audit`, `verification pass`, `confirmed adjusted refuted`, `refuted`, `judge panel`, `verify volatility`, `report the verdict` (2026-07-21)
- [[knowledge-vault-design]] — Why three surfacing tiers exist, why entries ≠ skills, how to extend the system. Triggers: `knowledge vault`, `surfacing tiers`, `extend the vault` (2026-04-29)
- [[skill-ecosystem-and-mcp-servers]] — Evaluation of skills.sh + mcpmarket.com: the three buckets (tool integrations / already-covered / genuine gaps), what we added (science math+physics, mobile, security), dedup decisions, attribution convention, recommended MCP servers (Blender for Legion, Postgres). Triggers: `mcpmarket`, `skills.sh`, `mcp server`, `blender`, `add skills`, `skill library` (2026-06-16)
- [[visual-failure-mode-ledger]] — The externalized memory behind framework #11 Anticipatory Failure Analysis + the `failure-mode-premortem` skill: technique-keyed rows (symptom · visible tell · root cause · prevention · how-to-detect · ref/tier) for how visual techniques classically fail and how to catch them in a frame. Domain-agnostic; seeded from Legion rendering; grows via the self-improving loop. Triggers: `pre-mortem`, `failure mode`, `what could go wrong`, `classic symptom`, `banding`, `dither crawl`, `bloom ring`, `acceptance criteria`, `ready for review`, `pitfall ledger` (2026-07-14)
- [[visual-reference-replication-findings]] — Adversarial diagnosis: agents fail Literal UI recreation via VLM-as-measurement, Spirit-as-done, no Construction IR, unused prove gates. Justifies skill `visual-reference-replication` + ledger C-03…C-08 / Z-04–05. Triggers: `literal match`, `recreate this screen`, `inspired by rejected`, `construction ir` (2026-08-09)
- [[measured-visual-verdicts]] — The four conditions for a trustworthy visual verdict, each violated by the manual pipeline it replaced and caught on real data the day [[visual-prove-engine]] ran: cues **measured never declared** (LCARS claimed 15/16, measurement found 13/16 and refuted a claimed-pass silhouette at 0.0 foreground); thresholds **derived from scouting the reference** with the judging probes (the manual >= 0.85 SSIM demand was unattainable: the reference itself scores 0.338 in that rect); detector reliability **proven by planted-defect calibration** (32 cases, zero miss / zero false-fire; caught 4 engine bugs including phantom jerk from settle-tail centroid jitter and global-metric blindness to local defects); ledgers **cue-granular** so `newly_failing` surfaces inside an aggregate win (v2→v3 improved 0.50→0.81 while regressing 2 cues). Plus: rank agreement as external validity check; capture manifest as part of the verdict. Triggers: `measured verdict`, `prove this build`, `cuespec`, `visual prove`, `self-attested`, `planted defects`, `calibration suite`, `improvement ledger`, `newly failing`, `rank builds`, `visual regression gate` (2026-08-26)
- [[agentic-domain-constitutions]] — **Same rigor as the DS constitution, for other job contexts.** Seven-layer remap is domain-agnostic; artifact graph is DSDS for design-systems and `domain-constitution/1.0` otherwise. Authored packs: UX/UI, motion, research, engineering, game, imaging/3D, vision, visual QA, illustration, architecture. Mapped only: product, data, security. Triggers: `domain constitution`, `job context ontology`, `ux ontology`, `game constitution`, `machine vision ontology` (2026-09-02)

---

_Add entries: create a file in the appropriate subdirectory, then add a line here._
_Format: `- [[filename]] — one-line summary (YYYY-MM-DD)`_
