# Core (required): the engine refuses to run without these
numpy>=1.24
Pillow>=10.0

# Optional accelerators (identical results, faster connected-components)
# scipy>=1.11
# opencv-python-headless>=4.8

# Optional (honest degrade; never required for Literal A)
# flip-evaluator>=1.0          # nvidia-flip; else flip-lite
# dreamsim + torch             # Spirit/NVS only
# pytesseract                  # plus system tesseract
# gltf-validator               # npm i -g; else stdlib parser
# vggt                         # geometry; else phase-correlation on >=2 views
