# S-SYS47-01 — Cue matrix (Literal)

Source: `captures/S-SYS47-01.png` (3840×2160, t=30s from System47 Enterprise-E Schematics MKV).  
Measurements: `S-SYS47-01.ir.json` + `S-SYS47-01.measure.json`.  
Build prove: `captures/S-SYS47-01_build_v4.png` + `captures/S-SYS47-01_side_by_side_v4.png` + `S-SYS47-01_build_v4.prove.json` (3840×2160 physical: 1920×1080 @ DPR 2, `?capture=1`, manifest-verified).

> **Live contract (2026-08-26): `S-SYS47-01.cuespec.json`**, proved by `visual-prove-engine`
> (`vqa prove build cuespec`). The status column in the table below is the 2026-08-09
> self-attested assessment, kept for history; measured verdicts live in
> `S-SYS47-01_build_v3.prove.md` and the verdict log. Where they disagree
> (row 14 silhouette), the measurement wins.

| # | Cue | Target | Tolerance | Status |
|---|---|---|---|---|
| 1 | Canvas aspect | 16:9 | exact | **pass** (1920×1080) |
| 2 | Background | `#000000` | Δe≤1 | **pass** |
| 3 | Inter-element gutter | **8px** at native (dense) | ±1px | **pass** (mode 7px @1080) |
| 4 | Header fill (bar interior) | `#4f93ca` @ (900,150) | Δe≤3 | **pass\*** (northstar `asset_header` plate; study-scale Δe provisional) |
| 5 | Header elbow drop fill | `#4c8fc1` @ (1700,180) | Δe≤3 | **pass\*** (in header plate) |
| 6 | Header bar thickness @ x=800 | **114px** native | ±4px | **partial** (plate scales with `--lcars-header-h`; full bay ~172px @1080 incl. drop) |
| 7 | Dual header elbows | SYSTEM 47 left + NCC-1701-E right, center cutout | structural | **pass** (sourceCrop plate) |
| 8 | Title string | `SYSTEM 47` ultra-condensed uppercase | exact text | **pass** (in plate) |
| 9 | Registry string | `NCC-1701-E` peach `#f4c285` | text + Δe≤5 | **pass** (in plate) |
| 10 | Hero schematic | Enterprise-E side MSD occupying center band | IoU/SSIM vs `asset_msd` ≥ 0.85 if raster | **pass\*** (mounted northstar MSD; SSIM toolkit not run) |
| 11 | Callout column | ≥4 deck/system labels with leaders on right of MSD | present | **pass** (baked in MSD plate + IR callouts) |
| 12 | Mid pill cluster | numeric pills under schematic | present | **pass** (`support.bay`) |
| 13 | Footer data band | dense readout row ~y≥1760 | present | **pass\*** (`asset_footer` plate; layout width still imperfect vs full still) |
| 14 | Silhouette chip | mini ship profile bottom-right | SSIM vs `asset_silhouette` or present | **pass** (in footer plate) |
| 15 | Chrome locked under motion | header elbows static; only footer greeble may tick | motion | **pass** (static plates; not re-proven motion) |
| 16 | Programmatic | Emitted via Scene IR `sys47.literal` + catalog/CSS pack — not orphan HTML | required | **pass** |

\* Raster plates are Construction IR `sourceCrop` assets under ADR-001 — Literal authority, not Galaxy AI / Flash SWF overrides.

## Verdict log

| Date | Verdict | Notes |
|---|---|---|
| 2026-08-26 | **Matches (measured 16/16, capture verified)** | v4 closes all three v3 failures; ledger movement `improved`, `newly_passing: [12,15,16]`, no regressions, score 1.0. Fixes: (1) surface clamped to 100vh with MSD/footer plates pinned at their Construction-IR source rects (cues 15/16 were failing because the composition ran 1206px tall and the footer band + silhouette sat below the fold); (2) capture moved to DPR 2 so physical pixels match the 4K reference natively, with `srcset` 1x/2x plate variants — at DPR 1 browser resampling blurred 8px reference gaps into 2px reads (cue 3); (3) MSD sourceCrop trimmed [419,270,2980,1210]→[419,270,2980,1120]: the wider crop leaked 50px of mid-band pill tops into the plate and printed a clipped pill strip under the schematic (found by eye, not by any cue — the SSIM window rose 0.89→0.93 after the trim); (4) mid band recomposed in `support.bay` (pills + CHI chip + separator ticks + right-aligned title band + 5 987 chip) per the reference grammar (cue 14). Capture provenance now attested by a sibling `.capture.json` manifest (viewport, DPR, png, reduced-motion freeze) — verdict upgraded from `partial` to `matches`. **Known residuals, all outside the current cue net (cuespec-v2 candidates):** the reference's fine left rail (x 0–11%) is not reconstructed; mid-band internal geometry is approximate (ref pills smaller, tiny numeric table + large title glyphs not replicated); the long navy sweep bar (ref y 1620–1665) and `11.30/AM` timestamp (y 1690–1745) are missing. |
| 2026-08-26 | **Partial (measured 13/16)** | First instrumented prove via `visual-prove-engine` (`vqa prove`). Cuespec: `S-SYS47-01.cuespec.json` (19 cues: 16 measured, 3 attested; targets scouted from the 4K reference). v3 = 13/16 measured pass, score 0.81. **Row 14's earlier "pass" is REFUTED**: silhouette zone measures 0.0 foreground (native crop pure black). Also failing: MSD region SSIM 0.195 (>=0.5) and footer density 20 regions (ref-derived [40,180]). v2 re-proved under same contract = 8/16, score 0.50; ledger `S-SYS47-01.ledger.md` records v2→v3 movement `improved` with `newly_failing: [15,16]`. Ranking `S-SYS47-01.compare-rank.json` agrees v3 > v2. The row-6 "±4px" claim is superseded by measured thickness 59px vs 62.5px target (pass at ±6 @1080). This measured verdict supersedes the self-attested rows above. |
| 2026-08-09 | **Partial** | Composite-guided refine: T2 content groups; identity/footer/MSD use northstar sourceCrop plates; support bay stays programmatic pills. Prove 15/16 structural at 1080; MAD vs study1920 still high (composition/scale mismatch). Gaps: instrumented SSIM/Δe at matched registration; programmatic elbows still fallback-only; footer plate letterboxing. Dev: `?northstar=S-SYS47-01&capture=1`. |
| 2026-08-09 | **Partial** | First live pass: Scene IR `sys47.literal` + measured fills (ADR-001) + sourceCrop MSD/silhouette rasters. Dual header labels present; gutter density=8. Gaps: constant-thickness header elbows still approximated; footer density/rhythm incomplete vs still; no SSIM/Δe instrumented prove yet; mid pills HTML + crop tightened. Build shot: `captures/S-SYS47-01_build_v2.png`. Open http://localhost:5175/?northstar=S-SYS47-01 |
