# Independent Planet Lab — live handoff

Updated 2026-09-12. Agent · Surface · Machine: Codex / Codex desktop / personal Mac, Apple M3 Max.

User requested a completely independent, additive experiment in a physically defensible dynamic planet generator, starting in Legion. All implementation and assets live in `~/Projects/Legion/planet-lab/`, with their own package, dependencies, local Git repository (branch `codex/independent-planet-lab`, commit `a367a99`) and ports 5187 (development) / 5188 (preview). No existing Legion source or configuration was changed.

Implemented: seeded reduced plate kinematics, uplift/ridges/trenches, bathymetry and climate proxies, geography-based settlement lights, spherical moisture transport, volumetric clouds/internal lightning, camera-relative Earth-scale rendering, continuous orbital descent to 8 m above the same terrain. Nine numerical checks and seven browser integration checks pass. Native shader validation passes.

Current visual evidence: `planet-lab/evidence/revision-2/`; older preserved run: `evidence/final/`; lightning diagnostic: `evidence/lightning/`. Read `docs/VISUAL-REVIEW.md` before describing fidelity. Verdict remains FAIL against the requested northstars: coarse coast/field geometry, contour-like bands and moire during insertion, inadequate near-ground landforms/materials, insufficient cloud-family morphology, synthetic city footprints, coarse lightning emission. No scientific correctness claim.

Performance: high 1920×1080 misses the 16.7 ms p95 target for some views. Balanced uses 1632×918 for a 1920×1080 viewport and passed five fixed poses at 8.8–9.2 ms p95, but descent reached 17.9 ms in a rolling window. Sustained 60 fps remains unproven. Final numbers and limitations live in `docs/BUDGET.md` and capture JSON. Do not generalize these results to other hardware, all locations, cold starts or full-resolution quality.

Native setup milestone (historical, before phase 1): authorized and implemented at `~/Projects/PlanetCompiler`, a separate personal-solo repository on `codex/planet-compiler-environment`, commit `b5f2f90`. Unreal 5.8.2 is installed at `/Users/Shared/Epic Games/UE_5.8`; Xcode 26.1.1 (17B100) at `/Applications/Xcode_26.1.1.app`; Metal Toolchain 17B54, CMake 4.3.4 and Ninja 1.13.2 are installed. Sean accepted the Xcode license and completed first launch. Project-specific scripts preserve the global CommandLineTools selection. Portable C++20 core tests pass 2/2 in Debug and Release. Unreal editor host compiled successfully in 11.9 s, repeat build in 2.8 s. Official MCP plugins enabled, localhost port8765 configured. Editor is open and the setup is verified end to end. MCP exposes 52 toolsets; initialization, tool discovery and `SceneTools.get_current_level` pass. Listener verified at 127.0.0.1:8765, including after a final restart. The installed 5.8.2 server returns blank serverInfo metadata despite documentation; retained as a known discrepancy in evidence. Final source disables the unused Android deployment plugin and excludes its automatically generated credential. Project-local Codex config loads when PlanetCompiler is opened as its own trusted project; this Legion task does not inherit it. At that setup milestone, no native material model or rendering port was implemented.

Current next action: execute the authorized bounded native phase-five camera selection and measured-readiness increment. The final section below is the live baton. Human acceptance and all original production fidelity gates remain open; Legion remains do-not-touch.

Do not touch the active Legion implementation or fold this experiment into it without Sean's instruction. Existing Legion dirty changes belong to other work. The main project handoff retains its own history; this scoped baton carries only the independent experiment.

## Native phase 1 — live handoff (2026-09-10)

Agent · Surface · Machine: Codex / Codex desktop / personal Mac, Apple M3 Max. Sean instructed resuming the handoff. Implemented and committed in the separate personal-solo `Projects/PlanetCompiler` repository on `codex/planet-compiler-environment`: `a3cc5f0` (controlled history core), `86ea71f` (finite rotation overflow guard), `2b470bf` (Unreal adapter and verified evidence), `a0d35bf` (reviewed scope-notice presentation correction). No remote is configured for this implementation repository. No Legion files changed.

Current outcome: portable C++20 core version 0.2.0 compiles spreading, subduction, collision, transform and rift histories using prescribed open spherical material strips. Recipes are complete and versioned; outputs carry formation intervals, explicit source/sink/deformation events and separate area/volume ledgers. Seed changes orient these fixtures; they do not generate varied geology. Unreal runs the fixed Release executable on a background worker through bounded StartCompile/GetCompileStatus/GetCompileResult/CancelCompile tools. One latest-job slot, a 30-second deadline plus bounded termination grace, and output/page caps are enforced. The editor module loads after engine initialization so the tools register correctly.

Executed evidence: Debug and Release each pass 3/3 core test suites and 65/65 independent executable checks; eight deliberately corrupted histories are detected in each configuration. The editor build/load succeeds; 9/9 native process/contract regressions pass. All 59 live integration checks pass and all five paged histories match direct Release output byte for byte. The longest recorded MCP call was about 395 ms on this Mac. Final listener is 127.0.0.1:8765. Retained upstream warnings: blank serverInfo metadata, and one engine r.MotionVectorSimulation render-thread warning in native-test output. Neither was substituted with inferred success.

Working set: `docs/INTENT.md`, `docs/MODEL.md`, `core/`, `tests/`, the Unreal host source, and `evidence/phase-1/`. The phase report records source/evidence hashes; original `evidence/setup-report.json` remains historical. Independent review corrected termination/pipe bounds, output typing, corruption-file hashing, parcel keyboard focus and chart scaling; final review found no remaining scope/provenance issue. The self-contained evidence page replays actual histories, permits parcel inspection, shows measured checks and a planted-failure gallery. Browser interaction/visual inspection is recorded; human acceptance remains pending Sean's inspection.

Runtime: Unreal remains open with the local compiler controls; no compiler job is running. An evidence-only viewer serves `evidence/phase-1` on 127.0.0.1:8770. All delegated implementation/review work is complete. The core worktree under `build/worktrees/core` is preserved; do not treat its untracked spec copy as new work.

Limits / next phase: no global tessellation or interacting topology, force-balanced dynamics, drainage, erosion, weather, terrain/collision renderer, packaged planet or fidelity/performance acceptance. Begin the coarse global model contract and controlled topology/event cases next; later production work must preserve the original scientific, reference-image and adversarial visual requirements. Existing Legion dirty changes and the independent browser prototype remain do-not-touch.

Presentation follow-up: removed the scope notice's decorative left accent after design-hook review. Browser inspection confirmed the change; the scoped detector and recorded evidence-integrity check passed. No suppressions or unresolved findings from that review; numerical/compiler code unchanged.

## Native phase 2 — completed checkpoint (2026-09-10)

Agent · Surface · Machine: Codex / Codex desktop / personal Mac, Apple M3 Max. Sean approved the connected globe phase with “Go ahead.” Implementation remains in the personal-solo `Projects/PlanetCompiler` repository on `codex/planet-compiler-environment`, with no remote configured. Final native checkpoint: `29744d6` (source/evidence integrity and intent checks pass; working tree clean). The phase-two intent is `docs/INTENT-PHASE2.md`; scientific and interface contracts are `docs/GLOBAL-MODEL.md` and `docs/GLOBAL-CONTRACT.md`. No Legion files changed.

Current outcome: portable core 0.3.0 compiles a conforming 20/80/320-cell spherical mesh with four prescribed plate motions. Each persistent material cohort retains its own birth plate's Euler transport, volume and formation-time moment. Conservative shared fluxes, explicit ocean sources/sinks, supported continental deformation, local thinning-limit markers and unresolved occupancy corrections have separate ledgers. Fixed cells carry mixtures; dominant-owner boundaries are diagnostic summaries. The four plate identities stay fixed. This is a bounded kinematic laboratory, not accepted geology or generated terrain.

Native outcome: global result validation and render preparation run in the external-compiler worker. Show/scrub/status/clear are bounded and tied to exact job/display identities. A transient native globe displays actual cells with plate, age and thickness colors, opaque depth-tested boundary ribbons and explicit 10-metre inspection radius. Actual Unreal Details controls start/stop recorded playback, reset the snapshot and switch data modes without dirtying the level. Playback uses an owned editor ticker, independent of background viewport actor throttling, and pauses during PIE. No persistent map/material, collision or terrain asset was created.

Executed evidence: Debug/Release each pass 5/5 core suites, 78/78 independent global checks and 65/65 strip regression checks. All 13 planted corruptions are detected in each independent global run. Native build/load succeeded; a clean editor passes 12/12 native tests including genuinely scheduled playback. Final global MCP probe passes 534 checks and all three preset histories match standalone Release bytes; strip MCP passes 59 checks across five histories. Eight real Slate UI checks pass. All six final images were independently inspected, with first/middle/final epochs, all three modes and an opposite view. `evidence/phase-2/phase-report.json` records source/artifact hashes; the evidence checker and intent gate audit the saved record. Human visual acceptance remains pending.

Review corrections: removed 153 subresolution breakup markers while preserving transport/deformation arithmetic; strengthened test coverage and bounded result paging; replaced rear-visible editor debug lines with opaque ribbons; moved playback from suppressed actor ticks to a dedicated editor scheduler. Hot reload retained an older automation-test registration; the initial 10/11 run and duplicate-registration log are preserved and superseded by the clean 12/12 run. `pre-scheduler-review/globe-plate-initial.png` preserves a black first capture. The unchanged probe after clean restart produced a readable initial frame; shader readiness is plausible but unconfirmed. No first-use rendering or exact playback-cadence guarantee is made. Pastel scene exposure and thin-edge aliasing remain in the diagnostic display.

Numerical limits: prescribed fast demo poles, authored recycling polarity, diffuse material interfaces and heuristic occupancy closure; no force balance or physically calibrated source-volume budget. The broad 10,000,000 m maximum-thickness bound is a numerical guard. New ocean on an initially continental birth tag cannot recycle under the default polarity matrix. A local breakup encounter does not create a plate or global rift topology. Independent spatial tracer L1 at 5 Myr decreases 0.00745677→0.00467817→0.00299338 over 20/80/320 cells; fixed-grid timestep solution differences have ratio 0.4960. These quantify numerical error, not geological fidelity. Default seed 1847 and independent oracle seed oracle-global-1847 have different placement/event counts and must not be conflated.

Runtime: current project editor serves only 127.0.0.1:8765 and is left showing the final 20 Myr plate view, playback stopped, level package clean. Temporary Slate observers were removed. The earlier clean editor instance released MCP but remained busy after TERM; it was force-closed only after its saved clean-package/no-open-assets evidence was reviewed. The current verified editor remains running. The phase-one evidence-only viewer at 127.0.0.1:8770 still serves historical phase-one content; it is not the new globe. Native screenshots are under `evidence/phase-2/`. The Mac was locked for OS-level automation; actual editor controls and screenshots were verified through Epic's own Slate tools. Preserved implementation worktrees under `build/worktrees/global-core` and `global-unreal` are complete; no agent work remains in flight.

Next action: Sean reviews the native diagnostic and saved evidence. A later bounded regional model should first specify surface/hydrologic conservation and a terrain hierarchy that rendering and collision share. Do not begin phase 3 or claim terrain, drainage, weather, a packaged application, photographic fidelity, new plate topology or production performance from this phase. Preserve all Legion work, original high-resolution references and adversarial visual requirements.

## Native phase 3 — completed checkpoint / live handoff (2026-09-12)

Agent · Surface · Machine: Codex / Codex desktop / personal Mac, Apple M3 Max. Sean authorized the next bounded region experiment with “Continue” and resumed it with “resume.” This authorizes implementation and verification, not human visual acceptance. All implementation remains in the personal-solo `Projects/PlanetCompiler` repository on `codex/planet-compiler-environment`, with no remote. No Legion files changed.

Current outcome: portable core 0.4.0 compiles one explicitly conditioned mountain-to-coast patch tied to a complete global recipe, epoch and cell. Nodal triangle control areas, physical ground separate from Priority-Flood routing, finite simple-lake fill/spill, quasi-steady runoff, zero-porosity sediment and separate erosion/deposition/export accounts are implemented. Plane, closed-basin and lake-spill fixtures expose limiting behavior. Source heights and coefficients are authored conditioning, not inferred geological elevation.

Native outcome: worker-side regional validation/preparation feeds cached native ground, supported water and receiver ribbons. Queries use the same exported triangle IDs and barycentric surface as the actual uploaded CPU mesh, with exact source/planet/native mappings. One metre maps to 100 centimetres without vertical exaggeration. The separate inspection origin is `(0,0,10000000)` cm, above the engine starter scene/cloud layer; it is not a planetary position. Explicit focus immediately targets actual ground bounds from an oblique direction. Show/scrub preserve camera and selection. Recorded playback uses the independent editor ticker. Collision remains disabled because the installed Unreal editor path synchronously cooks procedural collision; no persistent map/material asset was created.

Implementation checkpoints: frozen contracts `b93edcd`, `0b71713`, `bc772e5`; integrated core `f0bf700`; native region `234d8e8`; malformed-envelope assertion `18a762c`; scheduler fixture correction `a468110`; placement/focus correction `a112e1c`. Final native/evidence checkpoint is `a0fe2ae`; the implementation working tree is clean. Source/evidence integrity and all seven intent acceptance checks pass. Canonical integrity, links and workspace validators pass with no warnings. Final evidence is indexed by `evidence/phase-3/phase-report.json`; source and every retained artifact have recorded hashes. The intent is `docs/INTENT-PHASE3.md`, with scientific/API contracts in `REGION-MODEL.md` and `REGION-CONTRACT.md`.

Executed evidence: Debug and Release each pass 7/7 portable suites, 112/112 independent regional checks with 24 actual planted corruptions, 78/78 global regressions and 65/65 strip regressions. The public-API harness independently exercises nonzero water/solid boundary forcing, zero runoff, finite inventory exhaustion, lake sediment-displacement spill, piecewise hypsometry, analytic incision and authoritative queries. The final native build takes 10.55 s; all 17 native tests pass in 7.13 s, including registered render state, immediate focus, preserved user state and genuinely scheduled playback. Regional MCP passes 1,771 checks, five byte-equal histories and 90 independent queries, including the 64-quad/8-step case. Existing live global/strip regressions pass 522/59 checks with three/five byte-equal histories. Focus-only changes did not alter those models or interfaces. All eight actual Slate control checks pass, including start/advance/stop, Snapshot Index Reset to Default, stable reset and both data-mode selections. All eleven final 2027×1090 native views pass independent original-pixel diagnostic review. Human visual acceptance remains pending.

Review corrections: conservative curvature bounds include negative ground; parent fraction validation tolerates bounded roundoff without changing metadata; native validation reconstructs physical budgets/storage and bounds accumulated retry work, coefficients and signed ledgers. Coastal water clips only real ocean-incident triangles at sea level inside the patch. Lake water remains the labeled fully-wet-triangle approximation. The first native 16/17 failure was a test fixture still calling/tracking the globe and is retained. The final fixture exercises the actual region scheduler.

Visual failure provenance: the first regional run passed 1,753 numerical/interface checks but all eleven images showed starter terrain or sky. Its report and images are preserved together in `initial-view-targeting-failure/`. Animated engine focus did not advance under background viewport throttling, and camera-directed placement put the patch about 19 km below world zero. Live repositioning confirmed visibility above the scene; the final deterministic translation and immediate focus fix leave existing scene lighting/assets unchanged. Independent review then caught two clipped corners in the first corrected top image; that image/metadata are retained in `top-framing-correction/`, and a 20% wider top frame passed reinspection. No numerical hash substituted for pixel inspection.

Numerical provenance caveat: the independent verifier overwrote its first 84/86 draft report. `initial-observed-diagnostics.json` is explicitly reconstructed from observed results without an original-report/binary hash claim. The original intermediate 104/106 Debug directory is preserved; its two failures were corrected oracle assumptions about land runoff versus direct ocean rain, with no core change. Final integrated reports and saved public-API harness source/executable bytes pass provenance checks.

Remaining limits: protected fixed spill rim and simple lake mask, perfect trapping, no porosity/groundwater/channel storage, no nested reservoirs or calibrated erosion parameters. Two tested parent seeds still fail the inherited global thickness guard. Time-refined analytic incision error is 5.821→2.884→1.435 cm; spatial lake capacity error is 4.025%→0.346%→0.111%; coupled morphology L1 improves only modestly (3.720→3.447 mm), with no asymptotic-order claim. Jagged lake edges, pastel exposure, thin-line aliasing/faintness and subtle morphology in wide images remain diagnostic limits. Background clouds are engine content. Captures require their mode/epoch/source metadata; no photographic fidelity, smooth-motion cadence, GPU/frame budget, terrain streaming, collision, global drainage or orbital landing continuity is accepted.

Runtime / working set: editor PID 21185 was last verified with a clean level, no playback, final 5000-year elevation display and only `127.0.0.1:8765` listening. Temporary Slate observer removed. Prior clean editor exited normally before the final build. Evidence, native source, scripts, model/render/budget contracts and intent are the working set. All implementation/review agents are complete; preserved region-core/region-unreal and earlier worktrees remain untouched. The old browser viewer on port 8770 is historical phase-one content, not this regional diagnostic.

Next action: Sean reviews the native diagnostic/evidence. Phase three is complete within the bounded contract; do not begin a broader phase without a new scoped instruction. Future production work must first specify and validate the next physical/numerical and shared terrain hierarchy requirements, retain original high-resolution references and adversarial gates, and preserve Legion. The earlier side-accent hook finding remains fixed with no suppression or unresolved item.


## Native phase 4 — completed checkpoint / live handoff (2026-09-12)

Agent · Surface · Machine: Codex / Codex desktop / personal Mac, Apple M3 Max. Sean instructed Continue after phase-three completion. All implementation remains in personal-solo PlanetCompiler on `codex/planet-compiler-environment`, with no remote. This authorizes bounded implementation and verification, not human visual acceptance. No Legion files changed.

Outcome: portable core 0.5.0 derives stable regional quadtree tiles from one immutable 16/32/64-quad snapshot. Four-quad tiles share integer source nodes; explicit midpoint/corner stitches close mixed-detail edges. Exact triangle overlays measure conservative world-space vertical error for every stitch mask. Coarse, mixed, finest and error-budget selections support a finest-detail pin disk. Balancing re-applies error/pin requirements until stable, because child error bounds need not decrease. Source queries remain distinct from queries of the committed uploaded mesh.

Native outcome: external worker validation/preparation and a single owned asynchronous Chaos cook precede atomic display/collision publication. A pending replacement retains the old complete surface; busy, cancellation, stale identity, cleanup and invalid input remain bounded. Query and actual trace responses report source, display, collision and actual transform identities. Strict guards reject tiny child scaling and absolute-transform overrides. Actual Details controls switch Elevation/Detail/Error and immediately focus the patch without republishing the collision body or dirtying the level. Transient inspection remains one source metre to 100 native centimetres at `(0,0,10000000)` cm.

Source checkpoints: contract `160bb7b` / `4499f5c`, core `151107f` / `57ab8d3`, oracle `9e2fc1e`, native `b6fc8ae` / `75e8c06` / `8a9faa9`. Final native source `8a9faa9` is independently reviewed. Final native/evidence checkpoint is `e9593d3`; the implementation working tree is clean. The source/evidence audit and all seven intent criteria pass, including rejection of stale regression provenance and changed native coordinates with stale error metrics. Canonical generators, routing checks, integrity/link/workspace validators, first-wave detectors and negative validator fixtures also pass. The saved index is `evidence/phase-4/phase-report.json`; the working set includes INTENT-PHASE4, TERRAIN-CONTRACT, core/native terrain code, verifiers, render/budget guides and all phase-four evidence.

Executed: Debug and Release each pass 9/9 portable suites, 105/105 independent terrain checks with 22 detected planted corruptions and 21,900 independent geometric probes. Actual library probes add 1,120 source and 1,120 emitted-surface queries per configuration with stratified non-node/interior coverage and 21 public invariants. Prior regional/global/strip audits pass 112/78/65 checks per configuration, with 24/13/8 detected defects. Final incremental native build (three actions) took 9.56 s; clean editor passes 24/24 native tests in 11.40 s. Final live run passes 1,837/1,837 checks, 12 byte-equal outputs/actual uploaded meshes and 13,200 real engine collision probes (12,992 distinct). Maximum source-space collision/display difference is 1.7578162e-6 m; maximum actual world-space difference is 1.7578155e-6 m, below the 0.02 m gate. Eight actual Slate control checks and independent original-pixel inspection of all twelve 2027×1090 final captures pass. Human acceptance remains pending.

Evidence provenance: preserve the original frozen oracle and both amendments, first 90-check run, earlier vertex-first 105-check runs, initial 18/24 native failure and 297/298 zero-probe live attempt. Initial failures exposed a worker handshake path and root-relative placement assumption. The earlier 1,825-check / 13,200-probe normal-pose run passed before review found the subtle/absolute child-transform bypass. Latest native tests exercise rejection/restoration and a real collision hit; final 1,837-check run adds explicit actual-world-coordinate comparisons. All originals and source-only review history remain indexed separately. No failed run was silently replaced by a later result.

Measured limits: final twelve native cases observed 30.45–164.43 ms preparation, 323.48–331.08 ms asynchronous cook elapsed including editor scheduling, and 0.0847–0.2904 ms publication. These are sampled intervals, not CPU-only cooking costs or GPU/frame guarantees. A separate adversarial 64-quad fixture near ±20 km heights took 0.029–0.043 s for portable compile/validate/serialize; it is not representative mountain relief.

Remaining limits: the 32 km / N64 source has 500 m spacing; the hierarchy adds no new physical samples or geology. The 2 cm gate compares displayed approximation with collision, not coarse approximation with source. No cube faces, camera streaming, continuous geomorphing, cache stress, global rebasing, whole-planet landing, calibrated geology, photographic fidelity or sustained frame rate is accepted. Discrete changes may pop. Interpolated pastel colors soften exact leaf/error boundaries; static captures have no numeric legend and cannot prove temporal continuity. Cyan basin is diagnostic terrain color; clouds/horizon are engine background. Preserve original high-resolution references and adversarial production gates.

Runtime: editor PID 66415 owns only 127.0.0.1:8765, left on the 2,000-year N64 finest Elevation surface (revision fnv1a64-d6a88ef95a14ad17, source fnv1a64-fb22737dd29ca389). Display `0ee6a4a0-f44a-7909-2d49-8da5f138d1ad` and job `f8b6a8de-f449-17a5-9e20-22aff389609c` are live-instance IDs, invalid after restart. Level is clean, no replacement/PIE/open assets; temporary Slate observer removed. Actual terrain is selected with Terrain controls exposed and focused. Old browser port 8770 still serves historical phase-one content. G4/U4/V4/D4 worktrees remain preserved; no implementation agent work is in flight.

Next: Sean reviews the completed native diagnostic and evidence. No further implementation phase has started. Any later production step must separately establish source-detail, traversal/streaming and original scientific/photographic requirements. Earlier side-accent design finding remains fixed, with no suppression or outstanding finding. Legion and the independent browser prototype remain do-not-touch.


## Native phase 5 — active implementation / live handoff (2026-09-12)

Agent · Surface · Machine: Codex / Codex desktop / personal Mac, Apple M3 Max. Sean instructed Continue after completed phase four. This authorizes the next bounded camera-driven selection and readiness increment, not human visual acceptance. Native checkpoint `e9593d3` and all evidence remain preserved; phase-five contract/design checkpoint is `9edd216` on `codex/planet-compiler-environment`. No native remote, and no Legion changes.

Scope and contract: PlanetCompiler `docs/INTENT-PHASE5.md` and `docs/VIEW-CONTRACT.md`. Conservative projected correspondence includes X/Y/Z binary32 centimetre upload, off-axis perspective and explicit near-plane uncertainty. The native controller observes one actual perspective render submission, retains one immutable source, and evaluates the committed surface separately from desired/accepted plans. One preparation OR cook and one coalesced newest view prevent queue growth and cancellation starvation. Equal source/partition reuses geometry; latest explicit mode survives publication. Selected finest leaves can remain uncertified. This is neither added geology nor whole-planet streaming/photographic/frame-rate acceptance.

Design review is complete and the exact field/rounding appendix is frozen before implementation. Intent gate/ready passed. Active isolated writers under PlanetCompiler `build/worktrees/`: G5 `view-core` / `codex/view-core` (terrain_core agent); U5 `view-unreal` / `codex/view-unreal` (terrain_native_review); V5 `view-oracle` / `codex/view-oracle` (evidence_review). Base `9edd216`; root coordinates integration. V5 must freeze its independent oracle from the contract before reading new implementation. No agent may control the editor; root serializes official localhost MCP calls. Runtime still has the prior phase-four editor; recheck identity/clean state before restart.

Next: finish G5/U5/V5, integrate commits, build/debug/release and independent regressions, then clean native restart/tests and real camera paths. Required evidence includes at least 100 actual submissions, ten geometry publications, 2000 actual collision probes within 2 cm across replacements, Start/Stop controls, lifecycle paths, original before/pending/after views and an independently inspected ordered capture sequence. Preserve unsuccessful runs. Phase-five acceptance remains unchecked until executed evidence exists. Update this live baton at completion. Human visual acceptance and original high-resolution/adversarial production requirements remain open; Legion and its independent browser implementation are do-not-touch.
